/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["res.cloudinary.com", 'tony.sardaritskillshare.com', 'via.placeholder.com'], // Add your domain here
  },
};

export default nextConfig;
